{
  "name": "webIII_PR02",
  "version": "1.0.0",
  "description": "Experimental project for web 3",
  "main": "src/index.html",
  "scripts": {
    "sync": "browser-sync start -s \"src\" -f \"src/**/*.*\"",
    "start": "npm run sync",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julia Schantz",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.24.7"
  }
}
